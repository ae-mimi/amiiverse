---
import NewsletterSignupBlock from "./NewsletterSignupBlock.astro";
import PlayerWidget from "../ui/PlayerWidget.astro";

const { widget_type } = Astro.props;
---

<section class="widget-section container">
    {widget_type === "newsletter" && <NewsletterSignupBlock />}
    {
        widget_type === "player" && (
            <PlayerWidget track={Astro.props.music_item} />
        )
    }
</section>

<style>
    .widget-section {
        height: auto; /* Ensure it fits content */
        padding-block: var(--space-md);
    }
</style>
