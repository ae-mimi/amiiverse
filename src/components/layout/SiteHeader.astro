---
import { Icon } from "astro-icon/components";

import { getGlobalSettings } from "../../utils/global";
const settings = getGlobalSettings();
const links = settings.nav || [];

// settings.nav is the source of truth for the main navigation.

const currentPath = Astro.url.pathname;
const normalizePath = (path: string) => path.replace(/\/$/, "") || "/";
---

<header class="site-header">
    <div class="header-inner">
        <!-- Left: Logo -->
        <a href="/" class="logo-link" aria-label="Amii Home">
            <img
                class="header-logo-img"
                src={settings.site_info?.logo_navy ||
                    "/assets/uploads/amii-logo_op-navy.webp"}
                alt={settings.site_info?.title || "Amii Logo"}
                loading="eager"
                width="98"
                height="59"
            />
            <style
                define:vars={{
                    logoNavy: `url(${settings.site_info?.logo_navy})`,
                    logoYellow: `url(${settings.site_info?.logo_yellow})`,
                }}
            ></style>
        </a>

        <!-- Center: Navbar -->
        <nav class="main-nav">
            <ul>
                {
                    links.map((link) => (
                        <li>
                            <a
                                href={link.href}
                                class={
                                    normalizePath(currentPath) ===
                                    normalizePath(link.href)
                                        ? "active"
                                        : ""
                                }
                            >
                                {link.label}
                            </a>
                        </li>
                    ))
                }
            </ul>
        </nav>

        <!-- Right: Actions -->
        <div class="header-actions">
            <!-- Social Icons (Desktop) -->
            <div class="header-socials">
                {
                    settings.socials?.map((social) => (
                        <a
                            href={social.url}
                            aria-label={social.platform}
                            target="_blank"
                        >
                            <Icon
                                name={`simple-icons:${social.icon}`}
                                class={`icon-social icon-${social.icon}`}
                            />
                        </a>
                    ))
                }
            </div>

            <!-- Mobile Menu Toggle (Contains both icons for perfect alignment) -->
            <button id="menu-toggle" aria-label="Toggle Menu">
                <div class="icon-container">
                    <Icon name="lucide:menu" class="icon-menu" />
                    <Icon name="lucide:x" class="icon-close" />
                </div>
            </button>
        </div>
    </div>

    <!-- Mobile Navigation Overlay -->
    <div id="mobile-menu-overlay">
        <!-- Close Button Removed (Reused Toggle) -->

        <nav class="mobile-nav">
            <ul>
                {
                    links.map((link) => (
                        <li>
                            <a
                                href={link.href}
                                class={
                                    normalizePath(currentPath) ===
                                    normalizePath(link.href)
                                        ? "active"
                                        : ""
                                }
                            >
                                {link.label}
                            </a>
                        </li>
                    ))
                }
            </ul>
        </nav>

        {
            settings.enable_follow_link && (
                <a href="/follow" class="mobile-follow-btn">
                    FOLLOW
                </a>
            )
        }

        <div class="mobile-socials">
            {
                settings.socials?.map((social) => (
                    <a
                        href={social.url}
                        aria-label={social.platform}
                        target="_blank"
                    >
                        <Icon
                            name={`simple-icons:${social.icon}`}
                            class={`icon-social icon-${social.icon}`}
                        />
                    </a>
                ))
            }
        </div>
    </div>
</header>

<style>
    /* Header Light Mode Layer */
    .site-header {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        min-height: 84px;
        background-color: var(--bg-header);
        padding: 24px 34px 0 34px;
        box-sizing: border-box;
        display: flex;
        justify-content: center;
        z-index: 1000;
        transition: transform 0.3s ease;
    }

    .header-inner {
        width: 100%;
        max-width: 1440px;
        margin: 0 auto;
        display: flex;
        justify-content: space-between;
        align-items: center;
        position: relative;
    }

    .main-nav {
        position: absolute;
        left: 50%;
        transform: translateX(-50%);
    }

    .main-nav ul {
        display: flex;
        gap: 40px;
        list-style: none;
        margin: 0;
        padding: 0;
    }

    .logo-link {
        display: block;
        line-height: 0;
    }

    .header-logo-img {
        width: 98px;
        height: auto;
        display: block;
        content: var(--logoNavy);
    }

    /* Dark Mode Toggle for Logo */
    :global([data-theme="deep-dive"]) .header-logo-img {
        content: var(--logoYellow);
    }

    .main-nav a {
        font-family: var(--font-body);
        font-style: normal;
        font-weight: var(--weight-regular);
        font-size: var(--text-xs);
        line-height: 16px;
        letter-spacing: 2px;
        color: var(--text-nav);
        text-decoration: none;
        text-transform: uppercase;
        font-stretch: 100%;
        transition: all 0.3s ease;
    }

    .main-nav a.active {
        font-weight: 600;
        color: var(--color-pink-500);
    }

    .main-nav a:hover {
        font-weight: 600;
        color: var(--text-nav);
        text-decoration: underline;
        text-decoration-color: var(--color-pink-500);
        text-decoration-thickness: 2px;
        text-underline-offset: 4px;
    }

    .header-actions {
        display: flex;
        align-items: center;
        gap: 24px;
    }

    .header-socials {
        display: flex;
        align-items: center;
        gap: 8px;
    }

    @media (max-width: 1024px) {
        .header-socials {
            display: none;
        }
    }

    .header-socials a {
        display: flex;
        justify-content: center;
        align-items: center;
        width: 33px;
        height: 33px;
        color: var(--text-nav);
        transition:
            transform 0.2s,
            opacity 0.2s;
    }

    .icon-social {
        width: auto;
        height: 20px;
        transition: transform 0.2s;
    }

    .icon-youtube {
        height: 22px;
        transform: scale(1.1);
    }

    .icon-social:hover {
        transform: scale(1.2);
    }

    .icon-container {
        position: relative;
        width: 24px;
        height: 24px;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .icon-menu,
    .icon-close {
        width: 24px;
        height: 24px;
        position: absolute;
        top: 0;
        left: 0;
        transition: opacity 0.3s ease;
    }

    .icon-close {
        opacity: 0;
    }

    /* Mobile Menu Toggle */
    #menu-toggle {
        display: none;
        z-index: 1001;
        position: relative;
    }

    /* Responsive Mobile */
    @media (max-width: 768px) {
        .site-header {
            height: auto;
            min-height: 54px;
            padding: 16px 8px;
            background-color: transparent;
            background-color: var(--color-yellow-500);
            border-bottom: none;
        }

        .main-nav {
            display: none;
        }

        .header-actions {
            gap: 16px;
        }

        /* 3. Hamburger/Close Toggle */
        #menu-toggle {
            display: block;
            background: none;
            border: none;
            cursor: pointer;
            padding: 0;
            color: var(--color-navy-500);
        }
    }

    /* Mobile Overlay Styles (Global) */
    #mobile-menu-overlay {
        position: fixed;
        top: 0;
        left: 0;
        width: 100vw;
        height: 100vh;
        background-color: var(--color-yellow-500);
        opacity: 0;
        pointer-events: none;
        display: none;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        gap: 27px;
        z-index: 999;
    }

    .mobile-nav ul {
        list-style: none;
        padding: 0;
        margin: 0;
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 27px;
    }

    .mobile-nav a {
        font-family: "Archivo", sans-serif;
        font-style: normal;
        font-weight: 400;
        font-size: 14px;
        line-height: 16px;
        letter-spacing: 2px;
        color: var(--color-navy-500);
        text-decoration: none;
        text-transform: uppercase;
        text-align: center;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .mobile-nav a.active {
        font-weight: 700;
        color: var(--color-pink-500);
    }

    .mobile-follow-btn {
        display: flex;
        justify-content: center;
        align-items: center;
        width: 130px;
        height: 55px;
        background: var(--color-navy-500);
        border-radius: 999px;
        font-family: var(--font-body);
        font-weight: 600;
        font-size: 15px;
        letter-spacing: 2px;
        color: var(--color-yellow-500);
        text-decoration: none;
        text-transform: uppercase;
    }

    .mobile-socials {
        display: flex;
        gap: 8px;
        margin-top: 16px;
    }

    .mobile-socials a {
        display: flex;
        justify-content: center;
        align-items: center;
        width: 33px;
        height: 33px;
        color: var(--color-navy-500);
        transition: transform 0.2s;
    }

    .mobile-socials a:hover {
        transform: scale(1.1);
    }
    /* Handled by Global Swap */
</style>

<script>
    import { gsap } from "gsap";

    // Mobile Menu Logic

    const menuToggle = document.getElementById("menu-toggle");
    const mobileMenu = document.getElementById("mobile-menu-overlay");
    const mobileLinks = document.querySelectorAll(".mobile-nav li");
    const mobileFollow = document.querySelector(".mobile-follow-btn");
    const mobileSocials = document.querySelector(".mobile-socials");

    const iconMenu = document.querySelector(".icon-menu");
    const iconClose = document.querySelector(".icon-close");

    let isMenuOpen = false;

    if (menuToggle && mobileMenu) {
        // Create GSAP Timeline
        const tl = gsap.timeline({ paused: true, reversed: true });

        // Animation Definition
        tl.to(mobileMenu, {
            duration: 0.4,
            opacity: 1,
            pointerEvents: "all",
            ease: "power2.inOut",
            display: "flex", // Ensure it's visible for animation
        })
            .to(
                iconMenu,
                {
                    opacity: 0,
                    rotation: 90,
                    duration: 0.2,
                },
                "<",
            )
            .to(
                iconClose,
                {
                    opacity: 1,
                    rotation: 0,
                    duration: 0.2,
                },
                "<",
            )
            .from(
                [mobileLinks, mobileFollow, mobileSocials],
                {
                    y: 20,
                    opacity: 0,
                    duration: 0.4,
                    stagger: 0.05,
                    ease: "power2.out",
                },
                "-=0.2",
            );

        // Toggle Logic
        menuToggle.addEventListener("click", () => {
            if (!isMenuOpen) {
                tl.play();
                document.body.style.overflow = "hidden"; // Prevent scrolling
                isMenuOpen = true;
                menuToggle.setAttribute("aria-label", "Close Menu");
            } else {
                tl.reverse();
                document.body.style.overflow = ""; // Restore scrolling
                isMenuOpen = false;
                menuToggle.setAttribute("aria-label", "Open Menu");
            }
        });
    }

    // Smart Sticky Header Logic
    const header = document.querySelector(".site-header");
    let lastScrollY = window.scrollY;
    let isHeaderHidden = false;

    if (header) {
        const handleScroll = () => {
            const currentScrollY = window.scrollY;

            // Show header if scrolling up or at the very top
            if (currentScrollY < lastScrollY || currentScrollY < 50) {
                if (isHeaderHidden) {
                    gsap.to(header, {
                        y: "0%",
                        duration: 0.3,
                        ease: "power2.out",
                    });
                    isHeaderHidden = false;
                }
            }
            // Hide header if scrolling down and past the top threshold
            else if (currentScrollY > lastScrollY && currentScrollY > 100) {
                if (!isHeaderHidden) {
                    gsap.to(header, {
                        y: "-100%",
                        duration: 0.3,
                        ease: "power2.in",
                    });
                    isHeaderHidden = true;
                }
            }

            lastScrollY = currentScrollY;
        };

        window.addEventListener("scroll", handleScroll, { passive: true });
    }
</script>
